# 部署检查清单

> 提交前逐项核对，确保演示环境可复现。

## 环境

- [ ] Python 3.10+ 已安装（实测 3.13 通过）
- [ ] `pip install -r requirements.txt` 成功
- [ ] 依赖版本确认：streamlit / curl_cffi / akshare / altair / pandas

## 配置

- [ ] `.env` 已创建（从 `.env.example` 复制）
- [ ] `ASCEND_API_KEY` 已填入有效 Key
- [ ] `ASCEND_MODEL` 确认为 `zai-org/GLM-5.2`

## 数据

- [ ] `data/offline/` 包含 5 个离线包（4 主题 + 概念列表）
- [ ] `data/offline/backtest_data.pkl` 存在（回测引擎交付后）
- [ ] `data/demo_state.json` 已生成（含四因子完整数据）

## 运行

- [ ] `python scripts/demo_prep.py --max-stocks 12` 成功生成 demo_state
- [ ] `streamlit run ui/app.py` 启动无报错
- [ ] 首页 + 5 个页面均可访问
- [ ] 预置热点秒开（<3秒）
- [ ] 输入「低空经济」实时分析 ≤5 分钟

## 测试

- [ ] `pytest tests/ -v` 全部通过（当前 30 个）
- [ ] 无回归（因子/风险/集成/离线包/批量接口）

## 演示前

- [ ] 演示录屏脚本已演练 ≥1 遍
- [ ] demo_state 已用最新数据生成
- [ ] 备用方案：现场网络异常时预置热点仍可秒开（离线包兜底）
